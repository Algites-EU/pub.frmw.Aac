# AAC Development Tools

Repository development tooling is a first-class artifact. Its Python product sources live under `src/product/python`, preparing the repository for artifacts that can carry multiple technology source trees under `src/product/<technology>`.

The artifact contains repository version synchronization, convention checking, bounded test execution, artifact building, and canonical capability-binding generation.
