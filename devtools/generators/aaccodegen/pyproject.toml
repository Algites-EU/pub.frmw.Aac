[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "algites-aaccodegen"
version = "1.0.dev0"
description = "Public shared code generators for Algites Application Components"
requires-python = ">=3.13"
dependencies = [
  "algites-aac-coreintf==1.0.dev0",
  "algites-aac-coreimpl==1.0.dev0",
  "PyYAML>=6",
  "jsonschema>=4.20",
]

[project.scripts]
aaccodegen = "algites.frmw.aac.codegen.cli:main"

[tool.setuptools.packages.find]
where = ["src/product/python"]
include = ["algites.frmw.aac.codegen*"]
namespaces = true

[tool.pytest.ini_options]
cache_dir = "../../../build/run/devtools/generators/aaccodegen/run/develop/pytest/cache"
testpaths = ["src/develop/python"]
addopts = "--import-mode=importlib"
